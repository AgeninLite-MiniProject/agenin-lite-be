meta {
  name: Untitled
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/invitations
  body: none
  auth: inherit
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

settings {
  encodeUrl: true
  timeout: 0
}
